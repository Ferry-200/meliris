已经拿到“阶段一/二/三”正向结果，剩下的重点是把对比做扎实、可复现、可解释。补充内容以“数据对齐、统计检验、消融与鲁棒性、工程与复现、论文结构”五个维度完善即可。

**数据整理与对比**
- 统一分割方案：固定 `train/val/test`，按歌曲级划分，确保三阶段用同一分割与同一子集，避免信息泄漏。
- 门限与迟滞统一：统一阈值寻优策略（如在验证集上用 `F1` 最大化），并报告不同阈值下的敏感性曲线。
- 指标齐全：除 `F1/Precision/Recall`，加入 `AUROC/PR-AUC/ECE（校准）/平均片段IOU/延迟(ms)/吞吐量(×实时)`。
- 统计显著性：对三阶段的指标用自助法或重采样计算置信区间，配对检验显著性与效应量。
- 问题集分层：按曲风/BPM/人声比例/合唱与独唱/有无强伴奏分层对比，避免“平均值掩盖差异”。

**后处理评估**
- 与无后处理对比：分别在“原始概率”“迟滞后”“迟滞+窗口约束（README 中方案）”三档对比冲突率、闪烁次数与有效展示时长。
- 参数敏感性：对 `m_start/t_near/min_len/t_gap_fill` 做网格或贝叶斯优化，报告最优点与鲁棒区间。
- 边界案例集：构建“边界近邻”“跨窗”“短中断”典型片段库，用片段级指标与可视化展示修正效果。
- 可定位引用：后处理流程与参数建议已写在 `d:\meliris\README.md:153–191`，补充“评估与消融”即可形成完整闭环。

**模型与标签消融**
- 标签质量对比：定量展示 `Y_SVD` 相对 LRC 的边界准确性（片段 IOU、边界偏差分布），支撑 `d:\meliris\README.md:110–112` 的三点结论。
- 架构消融：CNN-only、LSTM-only、CNN+LSTM 基线对比；卷积核大小、Mel 参数（`n_mels`, `hop_length`）与输入窗口长度的影响。
- 任务权重扫描：MTL 的 `λ1/λ2` 网格，验证“边际效益是否微小”，并给出“单任务更优”的工程结论。
- 损失与正则：类不平衡处理（`pos_weight/focal`）、标签平滑、Loss Masking 的有无，以及对边界错判的影响。

**鲁棒性与泛化**
- 噪声与混响：添加环境噪声、压缩伪影、混响，测稳健性曲线。
- 速度与调性：变速（±5–10%）与移调（±2–4 semitones）对效果影响。
- 外部数据：少量外部曲目作为外部测试集，报告跨域性能跌幅。
- 运行时稳健：长音频、稀疏人声、说唱/念白等场景的错误类型统计与案例分析。

**效率与工程化**
- 性能画像：参数量、FLOPs、CPU-only 与常见移动芯片的推理延迟；实时性结论（×实时）。
- 资源成本：Demucs 分离成本与缓存复用策略；端到端流水线总时延。
- 复现与环境：固定随机种子、依赖版本、显卡/CPU规格；导出 `requirements.txt`/环境快照，给出“一键评估脚本”。
- 产出清单：`raw→features→labels→splits→metrics.csv→figures/` 目录规范，保证核验与评审可直接运行。

**建议的图表与表格**
- 指标总表：三阶段在 `val/test` 的 `F1/P/R/AUROC/PR-AUC/ECE/Latency`。
- 阈值敏感性：`F1` 随阈值或迟滞参数变化曲线。
- 可靠性图：校准曲线与 ECE。
- 片段边界直方图：预测与真实边界差的分布。
- 任务权重热力图：MTL 的 `λ1/λ2` 对 `F1` 的影响。
- 误差分类堆叠条形图：边界近邻、弱人声、强伴奏、合唱等类型占比。
- 可视化案例：Mel 频谱上叠加 `p(t)`, `voice_ratio(t)` 与 LRC 窗口 `W_i`，展示后处理前/后对比。

**论文结构补充**
- 方法与创新点：强调“标签质量优先于结构复杂度”的核心结论与工程含义。
- 实验设计：数据、分割、指标、统计方法、消融计划与鲁棒性测试。
- 结果与讨论：三阶段对比、边际效益分析、何时 MT 单任务更优的边界条件。
- 威胁与限制：数据规模、曲风覆盖、Demucs 误差传播、LRC 时间戳偏差。
- 伦理与版权：音频数据的版权合规、分离与展示的使用范围。
- 工程落地：实时系统的参数建议与部署指南。

**下一步行动**
- 整理统一的 `splits` 与评估脚本，输出 `metrics.csv` 和图表。
- 跑完后处理三档对比，并做参数敏感性扫描。
- 完成标签质量与架构/任务消融，小样本外部集鲁棒性测试。
- 汇总统计显著性与置信区间，写入论文与 README 的“结果对比”章节。
- 打包复现清单与环境快照，准备答辩演示（含案例视频或交互式展示）。
